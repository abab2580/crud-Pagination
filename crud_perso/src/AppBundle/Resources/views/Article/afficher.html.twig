{% extends "base.html.twig" %}
 {% block stylesheets %}
     <link rel="stylesheet" href="{{ asset('css/style.css') }}"/>

 {% endblock %}

{% block title %}AppBundle:Article:afficher{% endblock %}

{% block body %}
<a href="{{ path('ajouter') }}">ACCEUIL
<h1> ma liste d'article en base de donnée</h1>
    <table>
        <thead>
        <tr>
            <th>Nom</th>
            <th>Categorie</th>
            <th>Date de commande </th>
            <th>Nombre</th>
            <th>Commentaire</th>
            <th>Modifier</th>
            <th>Supprimer</th>
            <th>Ajouter</th>
        </tr>
        </thead>
        {% for key,article in article %}
        <tbody>
        <tr>
            <td>{{ article.getNom() }}</td>
            <td>{{ article.getCategorie() }}</td>
            <td>{{ article.getDate|date("m/d/Y H:i") }}</td>
            <td>{{ article.getNombre() }}</td>
            <td>{{ article.getCommentaire() }}</td>


           <td><a href="{{ path('modifier', {'id':article.id} ) }}">modifier
               </a></td>
            <td><a href="{{ path('supprimer', {'id':article.id} ) }}">supprimer
                </a></td>
            <td><a href="{{ path('ajouter', {'id':article.id} ) }}">ajouter
                </a></td>
        </tr>

        </tbody>

        {% endfor %}

    </table>


{% endblock %}
